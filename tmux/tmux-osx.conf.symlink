set-option -g           default-command                 "reattach-to-user-namespace -l bash"
set-option -g           status-right                    " #(hostname)  #(battery)  %m|%d  %l:%M "
set-option -ga          update-environment              " ITERM_PROFILE"
